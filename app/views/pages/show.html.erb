<div class="col-md-7">
  <%= content_tag_for :article, @page do %>
      <header>
        <h1><%= @page.title %></h1>
      </header>
      <section class="body">
        <%= @page.body.html_safe %>
      </section>
  <% end %>
</div>
